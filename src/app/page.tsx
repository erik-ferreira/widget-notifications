import { Widget } from "@/components/Widget"

export default function Home() {
  return (
    <main className="flex items-center justify-center h-screen bg-zinc-50 dark:bg-black text-zinc-800 dark:text-zinc-50">
      <Widget />
    </main>
  )
}
